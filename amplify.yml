version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    #preBuild:
    #  commands:
    #    - aws s3 sync s3://${BUCKET_NAME}/ ./content/
    build:
      commands:
        - env
        - hugo --buildDrafts
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
